*.gem
*.rbc
.bundle
.config
.secrets
coverage
InstalledFiles
lib/bundler/man
pkg
ChefExtensionHandler/installer
rdoc
spec/reports
test/tmp
test/version_tmp
tmp
PESTER_SANDBOX

# YARD artifacts
.yardoc
_yardoc
doc/

# Local test environment files (contain secrets)
testing/.env
.env

*.pem

testing/artifacts/

# Dev/test bundle lockfile is regenerated per-Ruby-version in CI (bundler-cache);
# only the root, runtime-only Gemfile.lock is committed for BlackDuck SCA scanning.
gemfiles/test.gemfile.lock
